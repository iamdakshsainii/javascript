// String + something = String concatenation
// Number + Number = Addition
console.log("1" + 2);
console.log(1 + "2");
console.log(1+2+"2"); // first js check 1+2 => 3 and then string so convert and give 32
// make it better and use 
console.log((1+2)+"2"); 

// increment and decrement
// If used postfix, with operator after operand 
// (for example, x++), the increment operator increments
// and returns the value before incrementing.

// If used prefix, with operator before operand
// (for example, ++x), the increment operator increments 
// and returns the value after incrementing.